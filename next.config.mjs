/** @type {import('next').NextConfig} */
const nextConfig = {
  // The deck is a presentation surface; keep the dev overlay out of the frame.
  devIndicators: false,
  reactStrictMode: true,
};

export default nextConfig;
